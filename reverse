function reverseString(str){
    //basecase
    if(str === " " || str.length === 1){
        return str;
    }
    //calling reverseString
    return reverseString
    (str.substring(1)) + str[0];
}
console.log(reverseString("Utkrisht Upadhyay"));
